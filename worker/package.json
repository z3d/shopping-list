{
  "name": "shopping-list-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute shopping-list-db --file=schema.sql",
    "db:init:local": "wrangler d1 execute shopping-list-db --file=schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
